from setuptools import find_packages, setup

setup(
    name='obs2mkdocs',
    version='0.0.0',
    description='obsidian-type markdown to mkdocs-type markdown',
    author='bhbbbbb',
    packages=find_packages(),
)
